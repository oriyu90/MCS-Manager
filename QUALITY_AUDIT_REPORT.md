# MCS Manager v1.0.0 修正後品質監査レポート

- 監査日: 2026-08-14
- 対象: macOS版 MCS Manager v1.0.0
- 著作者: Yuki_Orita
- 対象領域: UI当たり判定、UI挙動、メモリリーク耐性、クラッシュ耐性

## 1. 結論

初回監査で確認した重大・高優先度の問題は修正済みです。厳格なSwift並行性ビルド、コアロジック11項目、Web画面の実ブラウザ操作、375px幅のレスポンシブ再測定を通過しました。

現時点で、通常利用を妨げる再現済みの重大問題は残っていません。ただし、実Minecraftサーバーを用いた24時間以上の長時間ソーク、OSからの強制終了・電源断、低速・悪意ある接続を大量に発生させる負荷試験は今回の検証範囲外です。これらはリリース後も継続すべき検証項目です。

### v1.0.0 UI再監査

- 日本語リソースが存在せず英語へフォールバックし得た構成を修正。`ja`を開発言語に設定し、日英両方のリソースをアプリ本体へ収録。
- Web管理画面の設定名、入力欄、結果通知、アクセシビリティ名を日英50キーで揃え、両言語に欠落がないことを検査。
- `NSOpenPanel.runModal()`を使っていた3箇所を共通の非同期パネルへ変更。メニューバーウィンドウの入れ子モーダルを全廃。
- ネイティブ画面を540×620pxへ拡張し、可変長サーバー名をヘッダーから分離。ユーザー追加欄を2段構成にして重なりを防止。
- ネイティブ/Webとも黒を基調とした青系高コントラストテーマへ変更。Web本文のコントラスト比は実測19.27:1。
- ネイティブタブの150ms全体アニメーションを除去。ユーザータブ初期表示時のJSON読込を4種類から表示中の1種類だけへ削減。
- Webタブは画面全体を再構築せず、タブ選択状態と内容パネルだけを更新。ローカルAPI込みの設定タブ表示は実測298ms。
- 375×812pxで`scrollWidth=clientWidth=375`、可視ボタンの重なり0件、全ボタン高さ44px以上、ブラウザ警告・エラー0件を確認。

## 2. 修正後分析計画

| 領域 | 故障仮説 | 測定方法 | 合格条件 |
|---|---|---|---|
| UI当たり判定 | 小型ボタン、重なり、横はみ出しにより操作不能になる | デスクトップ幅と375px幅で全可視ボタンの矩形を実測。削除ボタン、タブ、主要操作を実クリック | Web操作要素の高さ44px以上、横スクロールなし、操作の重なりなし |
| UI挙動 | 定期更新が入力を消す、認証再読込でログアウトする、削除確認がメニューバーUIを壊す | 設定編集中に5秒更新を跨ぐ、認証後に再読込、削除確認と言語切替を操作 | 入力値保持、セッショントークン再利用、インライン確認、日英切替成功、ブラウザエラー0件 |
| メモリリーク耐性 | タイマーや非同期監視が重複し、接続・ログ・サンプルが無制限に増える | 所有関係のコードレビュー、厳格並行性ビルド、上限・タイムアウト確認、傾向判定の合成データ試験 | 監視の同時実行1件、ログ/サンプル/接続/要求サイズに上限、安定メモリで誤警告しない |
| クラッシュ耐性 | 終了時にワールドを破損、クラッシュ後に二重起動、異常入力でアプリ停止 | 終了状態遷移、PID復旧、HTTP異常系、ポート変換、遅延タスク世代管理をレビュー・ビルド | 通常は`stop`を送信、30秒後のみ強制終了、残存PIDを検出、無効入力を4xxで拒否、並行性警告0件 |

## 3. 修正内容と評価

### 3.1 UI当たり判定

#### 修正済み

- Web画面の全ボタンに最低44pxの高さを設定。
- ホワイトリスト削除ボタンを44×44pxに拡大し、プレイヤー名を含む`aria-label`を追加。
- 375px幅で操作ボタンを横並び折返しにし、ステータスカードを1列表示に変更。
- ネイティブ画面の終了、一覧更新、プロファイル削除アイコンに明示的な28×28pxの操作領域と矩形の当たり判定を追加。
- 削除操作は、サーバー停止中だけ表示される設計を維持。

#### 実測結果

- デスクトップ幅: Webの可視ボタンはすべて高さ44px以上。
- 375×812px: Webの可視ボタンはすべて高さ44px以上。
- ホワイトリスト削除: 44×44px。
- 修正前の375px表示: `scrollWidth=443`, `clientWidth=375`で68pxの横はみ出しを検出。
- 修正後: `scrollWidth=375`, `clientWidth=375`。横はみ出しを解消。
- モバイル時の主要操作ボタンは各170×44px、サーバー選択行は341×63px。

#### 評価

Web UIはタッチ操作を含めて合格です。ネイティブUIの28pxはmacOSのポインタ操作向けとして十分な領域ですが、iOS相当の44px基準ではありません。ネイティブ画面は固定540×620pxのメニューバーウィンドウであり、タッチ操作対象ではないため仕様上の許容としました。

### 3.2 UI挙動

#### 修正済み

- Webの定期更新をステータスタブ表示中だけに限定し、設定入力の上書きを防止。
- 更新処理と操作処理に同時実行ガードを追加し、重複API要求と画面競合を防止。
- 非同期タブ読込は、完了時にサーバーIDと現在タブを再確認してから描画。
- 認証トークンを`sessionStorage`から復元し、再読込時はパスワード入力欄で空値上書きしないよう修正。
- ブラウザ標準`confirm`を廃止し、アクセシブルなインライン削除確認ダイアログへ変更。
- Web画面を単一サーバー・単一タブ中心の構成に整理。
- ネイティブ画面に英語リソースを追加し、メニューバーの言語設定をSwiftUIロケールへ反映。
- Purpur JARを初期登録時にも有効と判定。

#### 実操作結果

- 設定タブのMOTDを編集中に5.5秒待機しても入力値を保持。
- 認証後の再読込でログイン画面に戻らず、管理画面を再表示。
- 削除確認はインラインダイアログで表示され、JavaScript標準ダイアログは発生せず。
- 日本語からEnglishへ切替後、Settings / Whitelist等の主要表示を英語化。
- ブラウザのerror/warningログは0件。

#### 評価

主要な状態競合、認証再読込、削除確認、狭幅表示は合格です。OS標準ファイル選択パネル等、一部の動的メッセージは起動中の言語変更直後に再生成されるまで旧言語が残る場合がありますが、操作不能にはなりません。

### 3.3 メモリリーク耐性

#### 修正済み

- プロセスメトリクス取得に同時実行ガードを追加し、5秒タイマーより取得が長引いてもタスクを重ねない設計へ変更。
- TCP遅延測定に1秒の接続タイムアウトを設定。
- ログを最大2,000件に制限。
- メモリサンプルを直近10分に制限。
- HTTP同時接続を64件、ヘッダーを32KiB、本文を1MiB、接続時間を15秒に制限。
- 接続終了・失敗・タイムアウトの全経路で接続辞書から解放。
- HTTPサーバー停止時にリスナーと全接続をキャンセル。
- ホワイトリスト遅延処理に起動世代番号を付け、停止・再起動後に古いタスクが新プロセスへ書き込むことを防止。
- メモリリーク警告を単純増加判定から、2分以上・24サンプル以上・50MB/分以上・256MBまたは15%以上の増加・決定係数0.60以上という保守的な線形傾向判定へ変更。

#### ロジック試験結果

- バージョン/Java要件4件、メモリ傾向2件、名前入力5件の合計11件を独立コンパイルしたハーネスで確認し、全件成功。
- 安定した周期変動データではメモリ警告なし。
- 持続的な増加データでは警告あり。
- 厳格な並行性診断で製品コードの警告0件。

#### 評価

コード上の無制限増加と非同期処理の積み重なりは抑制されています。ただし、Minecraft/JVM自体のヒープ増加と本アプリのリークは別物です。表示は「確定したリーク」ではなく「持続的なメモリ増加傾向」として扱います。24時間以上の実測では、アプリの常駐メモリ、接続辞書件数、タスク数、タイマー数が定常化することを追加確認してください。

### 3.4 アプリのクラッシュ耐性

#### 修正済み

- アプリ終了時は全サーバーへMinecraftの`stop`を送信し、最大30秒待機してから未終了プロセスだけを強制終了。
- 実行中PIDをサーバーフォルダの`.mcsmanager.pid`へ保存。
- 次回起動時にPIDと実コマンドが対象サーバーフォルダに一致する場合、外部プロセスとして検出して二重起動を禁止。
- PID再利用で無関係なプロセスを誤検出しないよう、`ps`のコマンド行も照合。
- 外部プロセスは通常停止できないことを明示し、強制終了だけを提供。
- Web APIポートは`UInt16(exactly:)`で検証し、0または範囲外の保存値でクラッシュしないよう修正。
- HTTPの不正Content-Length、重複Content-Length、Transfer-Encoding、過大要求、不正コマンド、コマンド改行、プレイヤー名の区切り文字を拒否。
- Javaを`JAVA_HOME`、`/usr/libexec/java_home`、Homebrew、システムパスの順で探索し、対象Minecraft版の最低Javaメジャーを確認。
- JVM既定値より後にユーザー引数を置き、ユーザーの`-Xmx`等を有効化。既定ヒープは物理メモリの半分、1–8GBに制限。

#### ビルド・レビュー結果

- Debugビルド成功。
- `-warn-concurrency -enable-actor-data-race-checks`付きビルド成功、製品コード警告0件。
- Web JavaScript構文検査成功。
- 実ブラウザの操作中エラー/警告0件。
- `try!`、`fatalError`、未検証の直接的な`UInt16`変換は製品コード内に残っていないことを静的確認。

#### 評価

通常終了、API異常入力、監視の並行性、クラッシュ後の二重起動防止は大幅に改善しています。残る設計上の制約は次の通りです。

1. OSの強制終了、電源断、`SIGKILL`ではMinecraftの正常停止を保証できません。
2. 外部プロセスとして復旧したJavaサーバーには標準入力を再接続できないため、アプリからの正常な`stop`は送信できません。
3. `start.sh`を使う構成では、スクリプト内で選択されるJavaをアプリが完全には検証できません。
4. Web APIはLAN向けのHTTPです。インターネット公開時は強いパスワードに加え、HTTPSリバースプロキシ、VPN、またはアクセス元制限が必要です。

## 4. バージョン互換性

| Minecraft | 必要Java | 判定 |
|---|---:|---|
| 1.20.0–1.20.4 | 17以上 | 自動検出・試験済み |
| 1.20.5–1.20.x | 21以上 | 自動検出・試験済み |
| 1.21.x | 21以上 | 自動検出・試験済み |
| 26.x | 25以上 | 年ベース表記として自動検出・試験済み |

Paper、Spigot、Purpur、`server.jar`、および`start.sh`を認識します。未知のファイル名でバージョン履歴にも情報がない場合はUnknownとなり、通常JAR起動ではJava要件の自動判定精度が下がります。

## 5. 検証証跡

| 検証 | 結果 |
|---|---|
| 通常Debugビルド | 成功 |
| 厳格並行性ビルド | 成功、製品コード警告0件 |
| コアロジック独立ハーネス | 11/11成功 |
| Web JavaScript構文 | 成功 |
| Web認証・再読込 | 成功 |
| 設定入力の更新競合 | 5.5秒経過後も保持 |
| Web 375×812px | 横はみ出しなし、全可視ボタン高さ44px以上 |
| 削除確認 | インライン表示、標準ダイアログなし |
| 日英切替 | 主要表示で成功 |
| ブラウザconsole warning/error | 0件 |

XCTestはローカルのCommand Line Toolsにモジュールが含まれず起動できなかったため、テストターゲットを製品へ残さず、同じ実装ファイルを直接コンパイルする独立ハーネスで代替しました。

## 6. 推奨する継続試験

1. Paper、Purpur、vanilla、独自`start.sh`を各1台ずつ用意し、起動・停止・再起動・クラッシュ復旧を反復する。
2. 24–72時間のソーク中にアプリRSS、接続数、タスク数、ログ件数、メトリクス件数を1分間隔で記録する。
3. 64接続上限、15秒タイムアウト、32KiBヘッダー、1MiB本文の境界値を専用HTTP負荷ツールで試験する。
4. サーバー保存中、ワールド生成中、プラグイン停止遅延中にアプリ終了を行い、30秒待機と強制終了への遷移を確認する。
5. macOS 13、14、15およびApple Silicon/Intelの配布実機でDMGインストール、初回起動、通知権限、メニューバー表示を確認する。

## 7. リリース判定

判定: **条件付きリリース可**。

今回再現できた重大・高優先度問題は解消され、コンパイル、並行性、コアロジック、Web操作の回帰確認を通過しています。インターネットへ直接HTTP公開しないこと、および実Minecraft環境で長時間ソークを継続することを運用条件とします。
